type Row = Record<string, any>;
const ID = "akre-radar-live";
const esc = (v: unknown) => String(v ?? "—").replace(/[&<>\"]/g, (c) => ({ "&":"&amp;", "<":"&lt;", ">":"&gt;", '"':"&quot;" }[c]!));
async function json(path:string, init?:RequestInit){const r=await fetch(path,{credentials:"same-origin",...init});const t=await r.text();let b:any=null;try{b=t?JSON.parse(t):null}catch{}if(!r.ok)throw new Error(b?.error||b?.detail||`Request failed (${r.status})`);return b;}
function cls(p:Row, obs:Row[]){return String(obs.find(o=>o.productName===p.name)?.payload?.productClassification??"AMBIGUOUS");}
async function render(){
 const radar=location.pathname==="/radar"||location.pathname==="/"; const ws=document.querySelector<HTMLElement>("fieldset.workspace");
 if(!radar||!ws){document.getElementById(ID)?.remove();if(ws)ws.style.display="";return;} ws.style.display="none";
 let root=document.getElementById(ID);if(!root){root=document.createElement("section");root.id=ID;ws.insertAdjacentElement("beforebegin",root);}
 try{const s=await json("/api/state"),d=s.data??{},products:Row[]=d.products??[],obs:Row[]=d.observations??[],opp:Row[]=d.opportunities??[],offers:Row[]=d.offers??[],costs:Row[]=d.costs??[];
  const liveObs=obs.filter(o=>o.sourceId==="google-trends");
  const candidates=products.filter(p=>liveObs.some(o=>o.productName===p.name)&&cls(p,liveObs)==="PRODUCT").sort((a,b)=>Number(liveObs.find(o=>o.productName===b.name)?.signalValue??0)-Number(liveObs.find(o=>o.productName===a.name)?.signalValue??0));
  const selected=candidates[0];
  root.innerHTML=`<div class="radarCommand"><section class="radarHero"><div><div class="smallcaps">COMMERCE COMMAND CENTER · INDIA</div><h2>Fresh products from live search demand</h2><p>AKRE now shows only candidates created from the current Google Trends feed. Existing manual products cannot occupy this list.</p><div class="actions"><button data-refresh>Refresh live India trends</button>${selected?`<button data-suppliers>Find suppliers for ${esc(selected.name)}</button>`:""}</div></div><div class="radarGate"><span>PIPELINE</span><b>LIVE TREND</b><i>→</i><b>PRODUCT</b><i>→</i><b>SUPPLIER</b><i>→</i><b>ECONOMICS</b><i>→</i><b>TEST</b></div></section><section class="metrics"><div class="metric"><span>Fresh Google Trends signals</span><strong>${liveObs.length}</strong><small>India · past 24h feed</small></div><div class="metric"><span>Product candidates</span><strong>${candidates.length}</strong><small>Strict product intent</small></div><div class="metric"><span>Supplier offers</span><strong>${offers.length}</strong><small>Recorded evidence</small></div><div class="metric"><span>Economics ready</span><strong>${candidates.filter(p=>offers.some(o=>o.productId===p.id)&&costs.some(c=>c.productId===p.id)).length}</strong><small>Supplier + cost</small></div></section><div class="contentgrid"><section class="panel"><div class="panelTitle"><h2>New product candidates</h2><span class="badge source-evidence">GOOGLE TRENDS</span></div>${candidates.length?candidates.slice(0,15).map((p,i)=>{const o=opp.find(x=>x.productId===p.id),ob=liveObs.find(x=>x.productName===p.name);return `<div class="radarCandidate ${i===0?"selected":""}"><span class="radarRank">${i+1}</span><span class="radarCandidateMain"><b>${esc(p.name)}</b><small>${esc(p.category)} · ${Number(ob?.signalValue??0).toLocaleString("en-IN")} approx searches</small></span><span class="radarScore">${esc(o?.scoring?.score??"—")}</span><span class="radarStatus">NEW</span></div>`}).join(""):`<div class="empty">No fresh product-intent candidates were returned. Click refresh to run the live feed again.</div>`}</section><section class="panel"><div class="panelTitle"><h2>Decision gate</h2><span class="badge">${selected?"NEW CANDIDATE":"WAITING"}</span></div>${selected?`<div class="radarProductHead"><div class="productIcon">${esc(String(selected.category||"?").slice(0,1))}</div><div><div class="smallcaps">${esc(selected.category)}</div><h3>${esc(selected.name)}</h3><p>LIVE Google Trends → PRODUCT</p></div></div><div class="radarSteps"><div class="radarStep done"><b>01</b><span>Live trend</span><strong>FOUND</strong></div><div class="radarStep done"><b>02</b><span>Product intent</span><strong>PRODUCT</strong></div><div class="radarStep ${offers.some(o=>o.productId===selected.id)?"done":"wait"}"><b>03</b><span>Supplier</span><strong>${offers.some(o=>o.productId===selected.id)?"FOUND":"MISSING"}</strong></div><div class="radarStep ${costs.some(c=>c.productId===selected.id)?"done":"wait"}"><b>04</b><span>Economics</span><strong>${costs.some(c=>c.productId===selected.id)?"READY":"MISSING"}</strong></div><div class="radarStep wait"><b>05</b><span>Test</span><strong>GATED</strong></div></div>`:`<div class="empty">No fresh product candidate is currently available.</div>`}</section></div><section class="panel radarEvidence"><div class="panelTitle"><h2>Supplier intelligence</h2><span class="badge">EVIDENCE SEPARATED</span></div><div id="${ID}-results"><div class="empty">Supplier matching starts after a fresh product candidate is found.</div></div></section><div class="radarFootnote">Search interest is demand evidence, not purchase proof. Supplier research is not live inventory until connected.</div></div>`;
  root.querySelector("[data-refresh]")?.addEventListener("click",async()=>{const b=root!.querySelector<HTMLButtonElement>("[data-refresh]");if(b){b.disabled=true;b.textContent="Refreshing…";}try{await json("/api/discovery/runs",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({provider:"google-trends",payload:{geo:"IN",category:"Unclassified",maxResults:50}}));await render();}catch(e){alert(e instanceof Error?e.message:"Discovery failed");}});
  root.querySelector("[data-suppliers]")?.addEventListener("click",async()=>{const r=root!.querySelector<HTMLElement>(`#${ID}-results`);if(!r||!selected)return;r.innerHTML='<div class="empty">Matching researched India suppliers and the live CJ catalog…</div>';try{const local=await json("/api/suppliers/catalogs/search",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({query:selected.name,category:selected.category})});let cj:any=null;try{cj=await json("/api/suppliers/cj/search",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({keyword:selected.name,countryCode:"IN",page:1,size:10})});}catch{}r.innerHTML=`<div class="contentgrid"><div><h3>Researched India suppliers</h3>${(local.suppliers??[]).map((x:any)=>`<div class="record"><b>${esc(x.name)}</b><p>${esc(x.status)} · ${esc(x.integration)}</p><small>${esc(x.evidence?.reviewSummary||"Evidence captured")}</small></div>`).join("")||'<div class="empty">No researched supplier match.</div>'}</div><div><h3>Live CJ catalog</h3>${(cj?.products??[]).map((x:any)=>`<div class="record"><b>${esc(x.name??x.nameEn??x.sku)}</b><p>${esc(x.sellPriceUsd??x.sellPrice??"—")} USD · stock ${esc(x.inventory??x.verifiedInventory??"unknown")}</p></div>`).join("")||'<div class="empty">CJ is not configured or no live match was returned.</div>'}</div></div>`}catch(e){r.innerHTML=`<div class="empty">${esc(e instanceof Error?e.message:"Supplier matching failed")}</div>`;}});
 }catch(e){root.innerHTML=`<div class="panel"><div class="empty">${esc(e instanceof Error?e.message:"Unable to load live radar")}</div></div>`;}}
const mo=new MutationObserver(()=>{if(location.pathname==="/radar"||location.pathname==="/")setTimeout(()=>void render(),150)});mo.observe(document.body,{childList:true,subtree:true});window.addEventListener("popstate",()=>void render());setTimeout(()=>void render(),700);
